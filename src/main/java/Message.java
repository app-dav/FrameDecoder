package main.java;

/**
 * An interface indicating a type of Message.  To add a message type,
 * simply implement this interface with a new class and add the additional
 * message type in the decodeMessage function within the MessageFrameDecoder class.
 */
public interface Message{}